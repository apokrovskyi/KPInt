<UserControl x:Class="KPInt.Controls.ValueInput.ValueInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:KPInt.Controls.ValueInput"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance local:ValueInputVM}">
    <StackPanel Orientation="Horizontal">
        <TextBlock x:Name="ValueLabel" VerticalAlignment="Center" Margin="10 0 10 0" MinWidth="30"/>
        <Slider x:Name="ValueSlider"
                Value="{Binding Value}"
                Width="100"
                IsMoveToPointEnabled="True"
                IsSnapToTickEnabled="True"
                TickFrequency="1"
                VerticalAlignment="Center"/>
        <TextBox x:Name="ValueTextBox" Width="30" VerticalAlignment="Center" Text="{Binding Value, Mode=OneWay}" />
    </StackPanel>
</UserControl>
