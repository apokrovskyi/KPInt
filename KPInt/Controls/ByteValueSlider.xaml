<UserControl x:Class="KPInt.Controls.ByteValueSlider"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d">
    <Grid>
        <StackPanel Orientation="Horizontal">
            <TextBlock x:Name="ValueName" VerticalAlignment="Center" Margin="10 0 10 0" Width="30"/>
            <Slider x:Name="ValueSlider" Minimum="0" Maximum="255" Value="{Binding Value, Mode=TwoWay}" Width="100" IsMoveToPointEnabled="True"
                    IsSnapToTickEnabled="True" TickFrequency="1" VerticalAlignment="Center"/>
            <TextBox x:Name="ValueTextBox" Width="30" VerticalAlignment="Center"/>
        </StackPanel>
    </Grid>
</UserControl>
